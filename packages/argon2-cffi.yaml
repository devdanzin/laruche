package: argon2-cffi
repo: https://github.com/hynek/argon2-cffi
pypi_url: https://pypi.org/project/argon2-cffi/
extension_type: pure
python_versions: []
install_method: pip
install_command: git fetch --tags --depth 1 && pip install -e ".[tests]"
test_command: python -m pytest tests
test_framework: pytest
uses_xdist: false
timeout: null
skip: false
skip_reason: null
skip_versions:
  '3.15': "Depends on argon2-cffi-bindings (C extension build failure on 3.15)"
notes: "Uses setuptools-scm (needs git tags)"
enriched: true
clone_depth: 50
import_name: argon2
