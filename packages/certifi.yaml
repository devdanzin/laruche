package: certifi
repo: https://github.com/certifi/python-certifi
pypi_url: https://pypi.org/project/certifi/
extension_type: pure
python_versions:
- '3.15'
- '3.14'
install_method: pip
install_command: pip install -e . && pip install pytest
test_command: python -m pytest
test_framework: pytest
uses_xdist: false
timeout: null
skip: false
skip_reason: null
notes: Very minimal test suite (3 tests). Tests use unittest but run via pytest.
enriched: true
