package: flower
repo: https://github.com/mher/flower
pypi_url: https://pypi.org/project/flower/
extension_type: pure
python_versions: []
install_method: pip
install_command: pip install -e . && pip install humanize
test_command: python -m tests.unit
test_framework: unittest
uses_xdist: false
timeout: 600
skip: false
skip_reason: null
skip_versions: {}
notes: 'Auto-enriched. Uses setup.py. Tests run via python -m tests.unit (tornado.testing). Runtime deps (celery, tornado, prometheus_client, pytz) installed with package.'
enriched: true
clone_depth: null
import_name: flower
