package: livekit-blingfire
repo: https://github.com/livekit/agents
pypi_url: https://pypi.org/project/livekit-blingfire/
extension_type: extensions
python_versions: []
install_method: pip
install_command: pip install -e livekit-plugins/livekit-blingfire/
test_command: python -m pytest livekit-plugins/livekit-blingfire/test_import.py
test_framework: pytest
uses_xdist: false
timeout: 600
skip: true
skip_reason: No real test suite; only a cibuildwheel import smoke test (test_import.py). The agents repo tests/ directory tests livekit-agents, not this C extension plugin.
skip_versions: {}
notes: 'C extension (pybind11/CMake) BlingFire bindings. Only has a cibuildwheel import verification test, not a test suite. fake_session and toxic_proxy in the original install were local test modules/wrong deps.'
enriched: true
clone_depth: null
import_name: null
