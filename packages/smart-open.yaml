package: smart-open
repo: https://github.com/piskvorky/smart_open
pypi_url: https://pypi.org/project/smart-open/
extension_type: pure
python_versions: []
install_method: pip
install_command: git fetch --tags --depth 1 && pip install -e ".[test]"
test_command: python -m pytest tests -p no:xdist -o "addopts="
test_framework: pytest
uses_xdist: true
timeout: null
skip: false
skip_reason: null
skip_versions:
  '3.15': "Depends on numpy (C ext build failure on 3.15)"
notes: "Uses setuptools-scm (needs git tags). xdist disabled for JIT testing. addopts cleared (refs: pytest-timeout)"
enriched: true
clone_depth: 50
import_name: null
