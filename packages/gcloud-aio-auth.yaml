package: gcloud-aio-auth
repo: https://github.com/talkiq/gcloud-aio
pypi_url: https://pypi.org/project/gcloud-aio-auth/
extension_type: pure
python_versions: []
install_method: pip
install_command: cd auth && pip install -e . && pip install pytest pytest-asyncio pytest-mock
test_command: cd auth && python -m pytest tests/unit/ -p no:xdist
test_framework: pytest
uses_xdist: false
timeout: 300
skip: false
skip_reason: null
skip_versions: {}
notes: Monorepo (talkiq/gcloud-aio). Auth subpackage in auth/ dir. Uses poetry build backend. Unit tests in auth/tests/unit/.
enriched: true
clone_depth: null
import_name: gcloud.aio.auth
