package: pyzmq
repo: https://github.com/zeromq/pyzmq
pypi_url: https://pypi.org/project/pyzmq/
extension_type: extensions
python_versions: []
install_method: pip
install_command: pip install -e . && pip install pytest
test_command: python -m pytest tests/ -k 'not test_socket'
test_framework: pytest
uses_xdist: false
timeout: 600
skip: false
skip_reason: null
skip_versions:
  '3.15': C extension (Cython) editable build fails on 3.15.
notes: ZeroMQ Python bindings. C extension (Cython). Some socket tests may need running
  zmq.
enriched: true
clone_depth: null
import_name: null
