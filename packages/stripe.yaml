package: stripe
repo: https://github.com/stripe/stripe-python
pypi_url: https://pypi.org/project/stripe/
extension_type: pure
python_versions: []
install_method: pip
install_command: pip install -e . && pip install pytest pytest-mock httpx anyio[trio] aiohttp
test_command: python -m pytest tests -p no:xdist
test_framework: pytest
uses_xdist: true
timeout: null
skip: false
skip_reason: null
skip_versions: {}
notes: "Uses xdist (addopts=-n auto), disabled for JIT testing. Needs httpx, anyio[trio], aiohttp, pytest-mock."
enriched: true
clone_depth: null
import_name: null
