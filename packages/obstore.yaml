package: obstore
repo: https://github.com/developmentseed/obstore
pypi_url: https://pypi.org/project/obstore/
extension_type: rust
python_versions: []
install_method: pip
install_command: pip install -e . && pip install pytest pytest-asyncio
test_command: python -m pytest tests/ -p no:xdist
test_framework: pytest
uses_xdist: false
timeout: 600
skip: true
skip_reason: Rust/maturin/PyO3 package — won't build until PyO3 supports 3.15
skip_versions: {}
notes: 'obstore is a Rust extension built with maturin/PyO3. Repo was incorrectly set to fsspec/filesystem_spec. Tests require cloud credentials (S3/GCS/Azure) and Docker for local minio. Skip until PyO3 supports 3.15.'
enriched: true
clone_depth: null
import_name: obstore
