package: diskcache
repo: https://github.com/grantjenks/python-diskcache
pypi_url: https://pypi.org/project/diskcache/
extension_type: pure
python_versions: []
install_method: pip
install_command: pip install -e . && pip install django pytest pytest-cov pytest-django pytest-xdist
test_command: python -m pytest -p no:xdist --override-ini="addopts=" tests/
test_framework: pytest
uses_xdist: true
timeout: 600
skip: false
skip_reason: null
skip_versions: {}
notes: 'Auto-enriched. Backend: setuptools. Uses xdist (disabled for JIT). models is local test module not pip package. tox.ini addopts override needed (--cov, -n auto).'
enriched: true
clone_depth: null
import_name: null
