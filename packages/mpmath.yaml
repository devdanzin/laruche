package: mpmath
repo: https://github.com/fredrik-johansson/mpmath
pypi_url: https://pypi.org/project/mpmath/
extension_type: pure
python_versions: []
install_method: pip
install_command: git fetch --tags --depth 1 && pip install -e . && pip install pytest pytest-timeout
  hypothesis
test_command: python -m pytest mpmath/tests/ -o "addopts="
test_framework: pytest
uses_xdist: false
timeout: 1200
skip: false
skip_reason: null
notes: Arbitrary-precision floating-point math. Uses setuptools-scm. Large test suite,
  some tests slow. addopts cleared (refs --doctest-modules, --doctest-glob, filterwarnings
  error::DeprecationWarning).
enriched: true
clone_depth: 50
import_name: null
