package: falcon
repo: https://github.com/falconry/falcon
pypi_url: https://pypi.org/project/falcon/
extension_type: extensions
python_versions: []
install_method: pip
install_command: FALCON_DISABLE_CYTHON=Y pip install -e ".[test]" && pip install pyyaml httpx uvicorn
test_command: python -m pytest tests/
test_framework: pytest
uses_xdist: false
timeout: 600
skip: false
skip_reason: null
skip_versions: {}
notes: 'Auto-enriched. Backend: setuptools. Has [test] extras. Tests in tests/. Uses Cython extensions (disabled via env var for JIT testing). Many optional test deps (cbor2, msgpack, etc.) handled with try/except.'
enriched: true
clone_depth: null
import_name: null
