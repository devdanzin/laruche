package: meson-python
repo: https://github.com/mesonbuild/meson-python
pypi_url: https://pypi.org/project/meson-python/
extension_type: extensions
python_versions: []
install_method: pip
install_command: pip install -e . && pip install pytest pytest-mock pytest-cov meson ninja cython build wheel pyproject-metadata
test_command: python -m pytest tests/
test_framework: pytest
uses_xdist: false
timeout: 600
skip: false
skip_reason: null
skip_versions: {}
notes: 'Tests build real packages so need meson+ninja in PATH (installable via pip). Tests use fixture packages in tests/packages/ (not PyPI deps). pyproject-metadata is a runtime dep.'
enriched: true
clone_depth: null
import_name: mesonpy
