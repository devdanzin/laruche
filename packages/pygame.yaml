package: pygame
repo: https://github.com/pygame/pygame
pypi_url: https://pypi.org/project/pygame/
extension_type: extensions
python_versions: []
install_method: pip
install_command: pip install pygame && pip install numpy
test_command: python -m pygame.tests --no-interactive
test_framework: unittest
uses_xdist: false
timeout: 600
skip: true
skip_reason: Requires SDL2 system library to build from source; no wheels for Python 3.14+; tests use python -m pygame.tests not pytest
skip_versions:
  '3.14': No wheels available for Python 3.14+; SDL2 system headers required to build from source
  '3.15': No wheels available for Python 3.14+; SDL2 system headers required to build from source
notes: 'Auto-enriched. Backend: setuptools. Tests use python -m pygame.tests (unittest-based), not pytest. Needs SDL_VIDEODRIVER=dummy SDL_AUDIODRIVER=disk env vars for headless testing.'
enriched: true
clone_depth: null
import_name: null
