package: aiobotocore
repo: https://github.com/aio-libs/aiobotocore
pypi_url: https://pypi.org/project/aiobotocore/
extension_type: pure
python_versions:
- '3.15'
- '3.14'
install_method: pip
install_command: pip install -e . && pip install pytest pytest-asyncio moto[all]
test_command: python -m pytest -m "not localonly" tests/ -p no:xdist
test_framework: pytest
uses_xdist: true
timeout: null
skip: false
skip_reason: null
skip_versions:
  '3.15': moto requires pydantic-core (Rust/PyO3) which doesn't support Python 3.15
    yet
notes: Tests need moto[server] which requires pydantic → pydantic-core (PyO3). Cannot
  run until PyO3 supports 3.15.
enriched: true
clone_depth: null
import_name: null
