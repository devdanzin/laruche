package: pyqt5-sip
repo: https://github.com/Python-SIP/sip
pypi_url: https://pypi.org/project/pyqt5-sip/
extension_type: extensions
python_versions: []
install_method: pip
install_command: git fetch --tags --depth 1 && pip install -e . && pip install pytest
test_command: python -m pytest test/
test_framework: pytest
uses_xdist: false
timeout: 600
skip: true
skip_reason: 'Test suite compiles C extensions at runtime via sip-build subprocess (conftest.py builds .so files with a C compiler); not viable without a full C toolchain and complex build setup.'
skip_versions: {}
notes: 'Auto-enriched. Backend: setuptools. Uses setuptools-scm. Tests in test/.'
enriched: true
clone_depth: 50
import_name: null
