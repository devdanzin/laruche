package: databricks-sqlalchemy
repo: https://github.com/databricks/databricks-sqlalchemy
pypi_url: https://pypi.org/project/databricks-sqlalchemy/
extension_type: pure
python_versions: []
install_method: pip
install_command: pip install -e . && pip install pytest sqlalchemy
test_command: python -m pytest tests/test_local/ -k "not e2e"
test_framework: pytest
uses_xdist: false
timeout: 600
skip: false
skip_reason: null
skip_versions:
  '3.15': Depends on databricks-sql-connector (build failure)
notes: 'Auto-enriched. Backend: poetry-core. tests/test/ needs a live Databricks connection and sqlalchemy test plugin. tests/test_local/e2e/ also needs a real DB. Only tests/test_local/ unit tests (ddl, parsing, types, do_ping) are runnable offline.'
enriched: true
clone_depth: null
import_name: null
