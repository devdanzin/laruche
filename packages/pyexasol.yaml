package: pyexasol
repo: https://github.com/exasol/pyexasol
pypi_url: https://pypi.org/project/pyexasol/
extension_type: pure
python_versions: []
install_method: pip
install_command: pip install -e . && pip install pytest
test_command: python -m pytest test/
test_framework: pytest
uses_xdist: false
timeout: 600
skip: true
skip_reason: All tests require a running Exasol database via Docker (integration tests only); no unit tests available
skip_versions:
  '3.15': Package requires Python <3.15
notes: 'Auto-enriched. Backend: poetry. Tests in test/. All tests are integration tests requiring a live Exasol DB via exasol-integration-test-docker-environment.'
enriched: true
clone_depth: null
import_name: null
