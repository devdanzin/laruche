package: pika
repo: https://github.com/pika/pika
pypi_url: https://pypi.org/project/pika/
extension_type: pure
python_versions: []
install_method: pip
install_command: pip install -e . && pip install pynose coverage gevent tornado twisted
test_command: python -m nose2 tests/unit
test_framework: nose2
uses_xdist: false
timeout: 600
skip: true
skip_reason: 'Acceptance tests require a live RabbitMQ broker on localhost:5672. Test runner is pynose/nose2, not pytest. Even unit tests are structured for nose2.'
skip_versions: {}
notes: 'Uses nose2/pynose as test runner (configured via nose2.cfg). Acceptance tests require RabbitMQ. Unit tests in tests/unit/, acceptance in tests/acceptance/.'
enriched: true
clone_depth: null
import_name: null
