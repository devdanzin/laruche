package: lz4
repo: https://github.com/python-lz4/python-lz4
pypi_url: https://pypi.org/project/lz4/
extension_type: c_extension
python_versions: []
install_method: pip
install_command: pip install -e . && pip install pytest
test_command: python -m pytest tests/ --ignore=tests/stream -o "addopts="
test_framework: pytest
uses_xdist: false
timeout: null
skip: false
skip_reason: null
notes: LZ4 compression with C extension. Uses setuptools_scm build backend. C extension
  wraps lz4 library. Stream tests ignored (need psutil). setup.cfg addopts has -x so
  override with -o addopts=.
enriched: true
clone_depth: 50
import_name: null
