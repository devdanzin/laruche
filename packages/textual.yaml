package: textual
repo: https://github.com/Textualize/textual
pypi_url: https://pypi.org/project/textual/
extension_type: pure
python_versions: []
install_method: pip
install_command: pip install -e . && pip install pytest pytest-asyncio pytest-xdist
  pytest-textual-snapshot time-machine linkify-it-py
test_command: python -m pytest tests/ -p no:xdist --ignore=tests/test_slug.py
test_framework: pytest
uses_xdist: true
timeout: 1800
skip: false
skip_reason: null
notes: TUI framework by Textualize. Uses poetry-core build backend. Large test suite
  with snapshot tests. Uses xdist by default, disabled for JIT. time-machine needed
  for time-dependent tests. Timeout set to 30min.
enriched: true
clone_depth: null
import_name: null
