package: duckdb
repo: https://github.com/duckdb/duckdb-python
pypi_url: https://pypi.org/project/duckdb/
extension_type: extensions
python_versions: []
install_method: pip
install_command: git fetch --tags --depth 1 && pip install -e ".[test]"
test_command: python -m pytest tests -o "addopts="
test_framework: pytest
uses_xdist: false
timeout: null
skip: false
skip_reason: null
skip_versions:
  '3.15': "C++ extension, complex build system, fails on 3.15"
notes: "Uses setuptools-scm (needs git tags). addopts cleared (refs: pytest-cov)"
enriched: true
clone_depth: 50
import_name: null
