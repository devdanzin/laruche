package: sspilib
repo: https://github.com/jborean93/sspilib
pypi_url: https://pypi.org/project/sspilib/
extension_type: extensions
python_versions: []
install_method: pip
install_command: pip install sspilib && pip install pytest
test_command: python -m pytest tests/
test_framework: pytest
uses_xdist: false
timeout: 600
skip: false
skip_reason: null
skip_versions:
  '3.15': No pre-built wheels for Python 3.15; source build requires Cython and sspi-rs on Linux
notes: 'Auto-enriched. Backend: Cython extensions. Tests in tests/. Using pip install (not -e .) to get pre-built wheels; experimental Linux support via sspi-rs bundled in manylinux wheels.'
enriched: true
clone_depth: null
import_name: null
