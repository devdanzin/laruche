package: tink
repo: https://github.com/tink-crypto/tink-py
pypi_url: https://pypi.org/project/tink/
extension_type: extensions
python_versions: []
install_method: pip
install_command: pip install tink && pip install pytest
test_command: python -m pytest tink/
test_framework: pytest
uses_xdist: false
timeout: 600
skip: false
skip_reason: null
skip_versions:
  '3.15': No pre-built wheels for Python 3.15; building from source requires Bazel and protoc
notes: 'C++ extension built with Bazel; pip install -e . requires Bazel and protoc. Use pip install tink from PyPI. Wheels available up to cp313.'
enriched: true
clone_depth: null
import_name: null
