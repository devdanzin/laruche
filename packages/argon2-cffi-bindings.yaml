package: argon2-cffi-bindings
repo: https://github.com/hynek/argon2-cffi-bindings
pypi_url: https://pypi.org/project/argon2-cffi-bindings/
extension_type: extensions
python_versions: []
install_method: pip
install_command: git fetch --tags --depth 1 && pip install -e . && pip install pytest
test_command: python -m pytest tests -p no:xdist
test_framework: pytest
uses_xdist: false
timeout: null
skip: false
skip_reason: null
skip_versions: null
notes: Uses setuptools-scm (needs git tags). Uses PEP 735 dependency-groups so pip install -e ".[tests]" does not work; install pytest manually. CFFI C extension (argon2 C library bindings).
enriched: true
clone_depth: 50
import_name: null
