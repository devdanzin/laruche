package: tox
repo: https://github.com/tox-dev/tox
pypi_url: https://pypi.org/project/tox/
extension_type: pure
python_versions: []
install_method: pip
install_command: git fetch --tags --depth 1 && pip install -e . && pip install pytest pytest-cov pytest-mock pytest-timeout pytest-xdist flaky re-assert
test_command: python -m pytest tests -p no:xdist
test_framework: pytest
uses_xdist: true
timeout: null
skip: false
skip_reason: null
skip_versions: {}
notes: "Uses hatch-vcs (needs git tags). Test deps in dependency-groups, not extras. xdist disabled for JIT testing"
enriched: true
clone_depth: 50
import_name: null
