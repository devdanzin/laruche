package: pgvector
repo: https://github.com/pgvector/pgvector-python
pypi_url: https://pypi.org/project/pgvector/
extension_type: pure
python_versions: []
install_method: pip
install_command: pip install -e . && pip install asyncpg django peewee pg8000 psycopg psycopg2 pytest pytest-asyncio scipy sqlalchemy sqlmodel
test_command: python -m pytest tests/
test_framework: pytest
uses_xdist: false
timeout: 600
skip: true
skip_reason: All tests require a live PostgreSQL instance with the pgvector extension installed; not runnable in CI without external infrastructure.
skip_versions: {}
notes: 'Backend: setuptools. Pure Python package. Skipped: requires live PostgreSQL + pgvector extension for all tests. playhouse is bundled in peewee (not a separate PyPI package); psycopg_pool is bundled in psycopg[pool]. Tests in tests/.'
enriched: true
clone_depth: null
import_name: null
