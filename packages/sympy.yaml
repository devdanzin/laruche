package: sympy
repo: https://github.com/sympy/sympy
pypi_url: https://pypi.org/project/sympy/
extension_type: pure
python_versions: []
install_method: pip
install_command: pip install -e . && pip install pytest hypothesis
test_command: python -m pytest sympy/ -k 'not slow'
test_framework: pytest
uses_xdist: false
timeout: 3600
skip: true
skip_reason: Test suite too large (>1hr even with -k 'not slow'). Timeout at 3600s.
notes: Symbolic math library. Uses setuptools build backend. Extremely large test
  suite. Tests co-located in sympy/ subdirs. Uses hypothesis for property-based tests.
  Slow tests excluded to keep runtime manageable. Timeout 1hr.
enriched: true
clone_depth: null
import_name: null
