package: argon2-cffi
repo: https://github.com/hynek/argon2-cffi
pypi_url: https://pypi.org/project/argon2-cffi/
extension_type: pure
python_versions: []
install_method: pip
install_command: git fetch --tags --depth 1 && pip install -e . && pip install pytest hypothesis
test_command: python -m pytest tests -p no:xdist
test_framework: pytest
uses_xdist: false
timeout: null
skip: false
skip_reason: null
skip_versions: null
notes: Uses hatch-vcs for versioning (needs git tags). Pure Python wrapper around argon2-cffi-bindings (the C extension). Uses PEP 735 dependency-groups so pip install -e ".[tests]" does not work; install pytest and hypothesis manually.
enriched: true
clone_depth: 50
import_name: argon2
