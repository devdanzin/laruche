package: igraph
repo: https://github.com/igraph/python-igraph
pypi_url: https://pypi.org/project/igraph/
extension_type: extensions
python_versions: []
install_method: pip
install_command: pip install -e . && pip install pytest pytest-timeout networkx numpy pandas scipy matplotlib cairocffi plotly Pillow
test_command: python -m unittest discover tests -v
test_framework: unittest
uses_xdist: false
timeout: 600
skip: true
skip_reason: C extension requiring cmake build of bundled igraph C library; heavy test deps (numpy, scipy, pandas, matplotlib) unlikely to have 3.15 wheels
skip_versions: {}
notes: 'Auto-enriched. Backend: setuptools+cmake. Tests in tests/. Test extras also include torch/torch_geometric (test-pyg) but those are optional.'
enriched: true
clone_depth: null
import_name: null
