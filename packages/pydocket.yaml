package: pydocket
repo: https://github.com/chrisguidry/docket
pypi_url: https://pypi.org/project/pydocket/
extension_type: pure
python_versions: []
install_method: pip
install_command: git fetch --tags --depth 1 && pip install -e .
test_command: python -m pytest tests -p no:xdist -o "addopts="
test_framework: pytest
uses_xdist: true
timeout: null
skip: false
skip_reason: null
skip_versions: {}
notes: "Uses setuptools-scm (needs git tags). xdist disabled for JIT testing. addopts cleared (refs: pytest-cov, pytest-timeout)"
enriched: true
clone_depth: 50
import_name: pydocket
