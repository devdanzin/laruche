package: django-redis
repo: https://github.com/jazzband/django-redis
pypi_url: https://pypi.org/project/django-redis/
extension_type: pure
python_versions: []
install_method: pip
install_command: pip install -e . && pip install pytest pytest-xdist pytest-mock pytest-pythonpath Django msgpack lz4 pyzstd
test_command: python -m pytest tests -p no:xdist -o "addopts="
test_framework: pytest
uses_xdist: true
timeout: 300
skip: false
skip_reason: null
skip_versions: {}
notes: "Redis cache backend for Django. Requires running Redis server for most tests. conftest.py imports xdist directly so pytest-xdist must be installed even though we disable it. addopts cleared (refs: pytest-cov)"
enriched: true
clone_depth: null
import_name: null
