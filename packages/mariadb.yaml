package: mariadb
repo: https://www.github.com/mariadb-corporation/mariadb-connector-python
pypi_url: https://pypi.org/project/mariadb/
extension_type: extensions
python_versions: []
install_method: pip
install_command: pip install -e . && pip install packaging
test_command: python -m unittest discover testing -v
test_framework: unittest
uses_xdist: false
timeout: 600
skip: true
skip_reason: 'Requires libmariadb system library (MariaDB Connector/C) to build, and a running MariaDB server for all tests.'
skip_versions: {}
notes: 'Backend: setuptools (C extension). Needs libmariadb system lib + MariaDB server. ipaddress is stdlib in Python 3.3+, not a PyPI dep. Tests in testing/test/.'
enriched: true
clone_depth: null
import_name: null
