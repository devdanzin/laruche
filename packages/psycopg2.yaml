package: psycopg2
repo: https://github.com/psycopg/psycopg2
pypi_url: https://pypi.org/project/psycopg2/
extension_type: extensions
python_versions: []
install_method: pip
install_command: pip install -e .
test_command: python -m unittest discover tests -v
test_framework: unittest
uses_xdist: false
timeout: 600
skip: true
skip_reason: Requires libpq-dev system library and pg_config to build; tests require a running PostgreSQL server (live external service)
skip_versions: {}
notes: 'testconfig and testutils are local test modules in tests/ (not PyPI packages). ipaddress is stdlib. Tests connect to a live PostgreSQL server via DSN from env vars.'
enriched: true
clone_depth: null
import_name: null
