package: progressbar2
repo: https://github.com/wolph/python-progressbar/
pypi_url: https://pypi.org/project/progressbar2/
extension_type: pure
python_versions: []
install_method: pip
install_command: git fetch --tags --depth 1 && pip install -e ".[tests]"
test_command: python -m pytest tests
test_framework: pytest
uses_xdist: false
timeout: null
skip: false
skip_reason: null
skip_versions:
  '3.15': "Uses removed typing_extensions.no_type_check_decorator"
notes: "Uses setuptools-scm (needs git tags)"
enriched: true
clone_depth: 50
import_name: progressbar
