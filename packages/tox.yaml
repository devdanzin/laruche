package: tox
repo: https://github.com/tox-dev/tox
pypi_url: https://pypi.org/project/tox/
extension_type: pure
python_versions: []
install_method: pip
install_command: git fetch --tags --depth 1 && pip install -e ".[test]"
test_command: python -m pytest tests -p no:xdist
test_framework: pytest
uses_xdist: true
timeout: null
skip: false
skip_reason: null
skip_versions: {}
notes: "Uses setuptools-scm (needs git tags). xdist disabled for JIT testing"
enriched: true
clone_depth: 50
import_name: null
