package: openai
repo: https://github.com/openai/openai-python
pypi_url: https://pypi.org/project/openai/
extension_type: pure
python_versions: []
install_method: pip
install_command: pip install -e . && pip install pytest pytest-asyncio respx dirty-equals
  inline-snapshot
test_command: python -m pytest tests/
test_framework: pytest
uses_xdist: false
timeout: 300
skip: false
skip_reason: null
skip_versions:
  '3.15': Depends on pydantic which requires pydantic-core (PyO3). Not buildable until PyO3 supports 3.15.
notes: OpenAI Python client. Has offline/mock unit tests using respx for HTTP mocking.
  Tests do not require an API key. Uses hatchling build backend.
enriched: true
clone_depth: null
import_name: openai
