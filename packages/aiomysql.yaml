package: aiomysql
repo: https://github.com/aio-libs/aiomysql
pypi_url: https://pypi.org/project/aiomysql/
extension_type: pure
python_versions: []
install_method: pip
install_command: git fetch --tags --depth 1 && pip install -e . && pip install -r requirements-dev.txt
test_command: python -m pytest tests
test_framework: pytest
uses_xdist: false
timeout: null
skip: true
skip_reason: All tests require a running MySQL server; conftest.py connects to 127.0.0.1:3306 at session scope with no mocking.
skip_versions: {}
notes: "Uses setuptools-scm (needs git tags). Requires live MySQL server for all tests."
enriched: true
clone_depth: 50
import_name: null
