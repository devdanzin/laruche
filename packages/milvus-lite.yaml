package: milvus-lite
repo: https://github.com/milvus-io/milvus-lite.git
pypi_url: https://pypi.org/project/milvus-lite/
extension_type: extensions
python_versions: []
install_method: pip
install_command: pip install -e . && pip install pymilvus pymilvus-model numpy pandas pytest
test_command: python -m unittest discover tests -v
test_framework: unittest
uses_xdist: false
timeout: 600
skip: true
skip_reason: pymilvus depends on grpcio and orjson which have no Python 3.15 wheels; build from source requires cmake; check/config/common/utils are local test modules not PyPI packages
skip_versions: {}
notes: 'Top-level tests (test_search.py etc.) use MilvusClient locally (no server). But pymilvus -> grpcio/orjson lack 3.15 wheels. check/config/simple/common/utils are local test dirs in tests/.'
enriched: true
clone_depth: null
import_name: null
