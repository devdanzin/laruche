package: matscipy
repo: https://github.com/libAtoms/matscipy
pypi_url: https://pypi.org/project/matscipy/
extension_type: extensions
python_versions: []
install_method: pip
install_command: pip install meson-python ninja numpy && pip install --no-build-isolation -e ".[test]"
test_command: python -m pytest tests/
test_framework: pytest
uses_xdist: false
timeout: 600
skip: false
skip_reason: null
skip_versions: {}
notes: 'Backend: meson-python (needs pre-install of meson-python+ninja+numpy before --no-build-isolation). [test] extras include pytest, pytest-subtests, pytest-timeout, sympy. matscipytest is a local tests/matscipytest.py file (not PyPI). atomistica/atomman/lammps/ovito are optional and guarded with try/except - tests skip gracefully without them.'
enriched: true
clone_depth: null
import_name: null
