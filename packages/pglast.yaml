package: pglast
repo: https://github.com/lelit/pglast
pypi_url: https://pypi.org/project/pglast/
extension_type: extensions
python_versions: []
install_method: pip
install_command: pip install -e . && pip install pytest pytest-cov
test_command: python -m pytest tests/
test_framework: pytest
uses_xdist: false
timeout: 600
skip: false
skip_reason: null
skip_versions: {}
notes: 'Backend: setuptools. No [test] extra (only dev). setup.cfg addopts uses --cov so pytest-cov needed. ast is stdlib, not PyPI. Tests in tests/.'
enriched: true
clone_depth: null
import_name: null
