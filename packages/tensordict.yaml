package: tensordict
repo: https://github.com/pytorch/tensordict
pypi_url: https://pypi.org/project/tensordict/
extension_type: extensions
python_versions: []
install_method: pip
install_command: git fetch --tags --depth 1 && pip install -e ".[tests]" && pip install h5py onnxruntime
test_command: python -m pytest test/
test_framework: pytest
uses_xdist: false
timeout: 600
skip: false
skip_reason: null
skip_versions: {}
notes: 'Backend: setuptools. Uses setuptools-scm. Has [tests] extras. Tests in test/. Removed bogus deps: ast (stdlib), functorch (merged into torch), pytorch (wrong name, use torch), streaming (not a package), ucxx (NVIDIA GPU-only). torch is a core dep installed via package.'
enriched: true
clone_depth: 50
import_name: null
