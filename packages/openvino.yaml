package: openvino
repo: https://github.com/openvinotoolkit/openvino
pypi_url: https://pypi.org/project/openvino/
extension_type: extensions
python_versions: []
install_method: pip
install_command: pip install -e . && pip install cpuinfo e2e_tests filelock install_pkg jax jax_layer_test_class jsonschema metrics_utils path_utils proc_utils pytest pyyaml requests tensorflow
test_command: python -m pytest tests/
test_framework: pytest
uses_xdist: false
timeout: 600
skip: true
skip_reason: Massive C++/CMake project; pip install -e . requires full CMake build. Install command contained fake internal module names (e2e_tests, install_pkg, jax_layer_test_class, metrics_utils, path_utils, proc_utils) that are not real PyPI packages. Tests require tensorflow/jax. Wheels only go up to Python 3.14.
skip_versions:
  '3.15': Wheels only available up to Python 3.14; test deps include tensorflow and jax (not available for 3.15)
notes: 'Auto-enriched. Backend: setuptools/CMake. Tests span e2e_tests, layer_tests, etc. Internal test utilities mistakenly listed as PyPI deps.'
enriched: true
clone_depth: null
import_name: null
