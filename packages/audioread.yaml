package: audioread
repo: https://github.com/beetbox/audioread
pypi_url: https://pypi.org/project/audioread/
extension_type: pure
python_versions: []
install_method: pip
install_command: pip install -e ".[test]"
test_command: python -m pytest test -o "addopts="
test_framework: pytest
uses_xdist: false
timeout: null
skip: true
skip_reason: Tests require audio decoding backends (FFmpeg, GStreamer, or pymad) to be installed as system libraries; without them all tests fail
skip_versions: {}
notes: "Tests use committed MP3 files in test/data/ but need an external audio backend to decode them. addopts cleared (refs: pytest-cov)."
enriched: true
clone_depth: null
import_name: null
