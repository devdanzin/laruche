package: datadog
repo: https://github.com/DataDog/datadogpy
pypi_url: https://pypi.org/project/datadog/
extension_type: pure
python_versions: []
install_method: pip
install_command: pip install -e . && pip install pytest pytest-cov mock vcrpy freezegun
test_command: python -m pytest tests/unit/
test_framework: pytest
uses_xdist: false
timeout: 300
skip: false
skip_reason: null
skip_versions:
  '3.15': JIT crash (_PyOptimizer_Optimize abort) during pip install.
notes: Datadog API client. Only run unit tests (tests/unit/) to avoid needing Datadog
  credentials. Tests use vcrpy for HTTP cassette recording and freezegun for time
  mocking. Uses setuptools build backend.
enriched: true
clone_depth: null
import_name: datadog
